web:
  host: 'localhost' # localhost   # Set `host` to `localhost` (instead of ``) to prevent the macOS firewall dialog.
  port: 8888
  username: test

directories:
  - slug: Pictures
    source: /Users/Manas/Pictures
    mode: edit
    trash: /Users/Manas/.Trash

